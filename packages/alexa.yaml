cloud:
  alexa:
    filter:
      exclude_domains:
        - group
        - remote
        - media_player
        - input_boolean
        - fan
        - sensor
      exclude_entities:
        - light.xiaomi_gateway
